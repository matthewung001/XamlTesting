<Window x:Class="XamlReview.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:XamlReview"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="100" />
            <RowDefinition Height="100" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.Resources>
            <ControlTemplate x:Key="RoundButtonTemplate" TargetType="{x:Type Button}">
                <Grid>
                    <Ellipse Width="50" Height="50" Name="ButtonBorder" Fill="Red"/>
                    <Ellipse Width="40" Height="40" Fill="GreenYellow"/>
                </Grid>
            </ControlTemplate>
        </Grid.Resources>

        <Button x:Name="startButton" Template="{StaticResource RoundButtonTemplate}" Grid.Row="0" Grid.Column="0">Start</Button>
    </Grid>
</Window>
